@Library('repo-creation') _

pipeline {
    agent any

    environment {

    }

    stages {
        stage('Creation') {
            steps {
                script {

                }
            }
        }
    }
}
